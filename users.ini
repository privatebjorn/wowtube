; ---------------------------------------------------------------------------
; WowTube user accounts
; Each section is a username.
; password  : plain text (this server is LAN-only, not internet-facing)
; quota_mb  : maximum disk space for this user's converted videos
; ---------------------------------------------------------------------------

[guest]
password = guest
quota_mb = 100

[user]
password = user
quota_mb = 500